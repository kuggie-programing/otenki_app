# Copy to .env only when you do not already have a .env.
# Keep your original production environment values. No real keys are included.
OPENWEATHER_API_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIMS_EMAIL=mailto:admin@example.com
# Empty = otenki_data.json beside main.py. On a host, use a persistent absolute path.
OTENKI_DATA_FILE=
# Empty = same-origin app. Set explicit frontend origins only for a split deployment.
OTENKI_ALLOWED_ORIGINS=
OTENKI_ENABLE_MONITORS=1
HOST=127.0.0.1
PORT=5000
